# Cross-review response: PR #82

- reviewer: Codex (gpt-5.6-terra, reasoning high, read-only), async lens
- verdict: BLOCK (advisory - does NOT block the merge; cross-review is never blocking)
- posted: `gh pr comment 82` on 2026-07-18

## Disposition

All three findings confirmed as real residual gaps and routed to a follow-up
issue with their own TDD, per the cross-review contract (a HIGH finding becomes
a new issue; it is never fed straight back into the maker):

- **#83** - SSRF hardening round 2:
  1. HIGH  - DNS-rebinding TOCTOU (resolve-then-reresolve) - was deferred in PR #82 body; reviewer escalated urgency.
  2. HIGH  - auth-header (`Authorization`) leak to cross-origin article links, no redirect required - widens PR #82's redirect-only deferral.
  3. MEDIUM - `192.0.0.0/24` special-use range passes `is_global` in Python 3.11.

No change requested on PR #82 itself: its scope (guard + redirect re-validation
shared across all fetch sites) is complete and correct for what #76 asked.

# Cross-review request: PR #82

- repo: sammyjdev/axon
- pr: https://github.com/sammyjdev/axon/pull/82
- issue: #76 - SSRF / local-file-read gap: unguarded fetch of follow_links article URLs in expansion transport
- tier: Legendary
- risk areas hit: security/SSRF (not literally named in loop.yaml's risk_areas keyword list - escalated per FORGE's false-escalate-over-false-clear rule; matches the FORGE #68 lesson in RULES.md)
- gate: 346 passed, 0 failed, 1 skipped (repo gate_cmd); tests/expansion validated manually (not covered by gate_cmd, tracked as #77): 48 passed, 0 failed, 7 pre-existing xfails; ruff clean
- reviewers already run: Claude Sonnet 5 (spec) APPROVE round 1; gpt-5.6-sol via codex exec (quality) BLOCK round 1 (CGNAT bypass, fixed) -> BLOCK round 2 (multicast/IPv6-reserved bypass, fixed) -> APPROVE round 3
- quench: mandatory battery (EMPTY_RETURN/NEGATE_CONDITIONAL/DROP_SIDE_EFFECT killed, IDENTITY_RETURN N/A) + 4 risk-area extras, all killed after fix rounds (2 initially surviving: opener-wiring not proven, multi-A-record DNS bypass not proven)

## What the maker was asked to do

- Add a URL-safety guard invoked before any fetch in the follow_links path (ideally shared with the primary fetch for defense in depth): scheme allowlist (http/https only); resolve the hostname and reject non-public IPs (loopback, link-local incl. cloud metadata, RFC1918/ULA private ranges, other reserved/unspecified ranges); apply the same check after following redirects.
- Tests: file:// rejected before any read; redirect from an allowed URL to an internal/loopback address blocked; IP-literal URLs (127.0.0.1, 169.254.169.254) rejected; existing follow_links happy-path tests unmodified and still passing.
- Update the `# noqa: S310` comments to reflect the new guard coverage.

## What to look for

Findings the same-family reviewers would be least likely to catch. Do not re-litigate style or re-run the gate - both already passed. Prefer: correctness under inputs the tests do not generate (unusual IP/hostname encodings, IDN tricks, IPv6-mapped IPv4 forms), security consequences of the chosen approach (the accepted DNS-rebinding TOCTOU limitation and the redirect-based header-leak note already flagged in the PR body as deferred follow-ups - a second opinion on whether either is actually more urgent than assessed would be valuable), and assumptions the diff makes about the environment (single-process module-level opener state, `asyncio.to_thread` concurrency).

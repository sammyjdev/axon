# Cross-review request: PR #95

- repo: sammyjdev/axon
- pr: https://github.com/sammyjdev/axon/pull/95
- issue: #83 - SSRF hardening round 2 (follow-up to #76/#82)
- tier: Legendary (security/SSRF risk_area)
- gate: tests/expansion 62 passed / 0 failed / 7 pre-existing xfails; ruff clean (S enforced). tests/expansion is NOT in gate_cmd; validated manually.
- reviewers already run: INCOMPLETE - the routed maker fell back to claude-sonnet-5 (gemma-4-31b split-maker subdir bug, claude-skills#3); the Legendary quality re-review round STALLED and never completed. Claude validated + shipped. This PR has had NO independent tier quality review - this cross-review is the primary second-vendor lens.

## What the maker was asked to do
Close three residual SSRF gaps from the #82 cross-review:
1. DNS-rebinding TOCTOU: pin the vetted IP, connect to it without re-resolving.
2. Cross-origin auth-header leak: strip source headers when the article URL's origin != source endpoint origin (and on cross-origin redirects).
3. 192.0.0.0/24 special-use range passing is_global.

## What to look for
Same-family reviewers never ran here, so scrutinize hard. Prefer: correctness of the IP-pinning handler (does it actually prevent re-resolution? Host header + TLS SNI/cert validation for https - does pinning break cert hostname verification or, worse, disable it?), completeness of the origin comparison (userinfo, IDN/punycode, port normalization, case), the header-strip allowlist vs any auth carried elsewhere (cookies, redirects that downgrade https->http), and any special-use range still missed. Do not re-run the gate.
